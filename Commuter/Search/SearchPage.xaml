<Page
    x:Class="Commuter.Search.SearchPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Commuter.Search"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    DataContext="{Binding Source={StaticResource Locator}, Path=SearchViewModel}"
    mc:Ignorable="d">

    <Page.BottomAppBar>
        <CommandBar>
            <AppBarButton Icon="Favorite" Label="Subscribe" Visibility="{Binding HasSelectedSearchResult, Converter={StaticResource VisibleWhenTrue}}" Command="{Binding Subscribe}"/>
        </CommandBar>
    </Page.BottomAppBar>
    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
        <Grid Margin="4" Visibility="{Binding HasSelectedSearchResult, Converter={StaticResource VisibleWhenFalse}}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>
            <StackPanel>
                <TextBox Margin="4" FontSize="20" PlaceholderText="Search" InputScope="Search"  Visibility="{Binding HasSelectedSearchResult, Converter={StaticResource VisibleWhenFalse}}" />
                <TextBlock Margin="12,4" Style="{StaticResource TitleTextBlockStyle}" Text="'Tech' Results"/>
            </StackPanel>
            <ListView Grid.Row="1" ItemsSource="{Binding SearchResults}" SelectedItem="{Binding SelectedSearchResult, Mode=TwoWay}">
                <ListView.ItemContainerStyle>
                    <Style TargetType="ListViewItem">
                        <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                    </Style>
                </ListView.ItemContainerStyle>
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <local:SearchResultControl />
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
        <local:DetailControl Margin="4" Visibility="{Binding HasSelectedSearchResult, Converter={StaticResource VisibleWhenTrue}}"/>
    </Grid>
</Page>
